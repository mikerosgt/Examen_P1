/* jQuery (local) placeholder.
 * Descarga la versión minificada oficial (p. ej. 3.7.1) y reemplaza este archivo:
 * https://jquery.com/download/
 * Nombre sugerido: jquery.min.js (dejar la misma ruta /js/jquery.min.js)
 */

// Pequeño "shim" para evitar errores si el archivo no se reemplaza inmediatamente
window.jQuery = window.$ = function(sel){ return document.querySelectorAll(sel); };
jQuery.fn = {};
